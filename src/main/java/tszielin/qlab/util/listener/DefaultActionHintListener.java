package tszielin.qlab.util.listener;

import javax.swing.Action;

public class DefaultActionHintListener extends ActionHintListener {
  public DefaultActionHintListener(Action action) {
    super(action);
  }
}
